'''
input list=[1,2,3,4,3,3,3,3,5] and gives output as =[1,2,3,4,5]
to check number is prime or not 
to print even numbers from list
to check number is palidrome(madam,nurses run) or not
'''

'''
# to find max of three numbers
def max(a,b,c):
    if(a>b):
        if(a>c):
            print(f"{a} is maximum.")
        # else:
        #     print(f"{c} is greater.")
            
    if(b>a):
        if(b>c):
            print(f"{b} is greater.")

    if(c>a):
        if(c>b):
            print(f"{c} is greater.")   
      
print("Ente the numbers respectively: ")
a=int(input("Enter the first number: "))
b=int(input("Enter the second number: "))
c=int(input("Enter the third number: "))
max(a,b,c)
'''


'''
# to sum all numbers in list
def check(list):
    sum=0
    for i in list:
        sum=sum+i
    print(sum)

li=[34,56,7,7,3,5]
LI=[5,6,2,1]
check(li)
check(LI)
'''
'''
# to multiply all numbers in list
def mul(list):
    mul=1
    for i in list:
        mul=mul*i
    print(f"multiply is: {mul}")
    
li=[2,3,4,5]
mul(li)
'''


'''
# to calculate factorial of number
while(True):
    def factorial(num):
        # fact=1
        # if(num==0 and num==1):
        #     fact=fact*num

        # else:
        #     fact=fact*(factorial(num-1))

        i=1
        fact=1
        while(i<=num):
            fact=fact*i
            i=i+1
        print(f"factorial is: {fact}")   

    n=int(input("Enter the number "))
    factorial(n)
    '''

# to check whether the number falls in given range
def check(n):
    pass
    \``````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````````























































